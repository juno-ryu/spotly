# 프로젝트 심층 분석 보고서

> **분석 일자:** 2026-02-07
> **프로젝트:** my (Next.js 스타터 프로젝트)
> **분석 범위:** 프로젝트 구조, 라이브러리 버전, 최신 트렌드, 코드 품질
> **프로젝트 룰:** `.claude/rules/global.mdc`, `architecture.mdc`, `new-feature-process.mdc` 반영

---

## 1. 프로젝트 개요

| 항목 | 내용 |
|------|------|
| **프로젝트명** | my (Next.js 스타터 프로젝트) |
| **프레임워크** | Next.js 15 + React 19 + TypeScript |
| **스타일링** | Tailwind CSS v4 + shadcn/ui (new-york 스타일) |
| **상태 관리** | Zustand 5 (클라이언트) + TanStack Query 5 (서버) |
| **폼 관리** | react-hook-form + Zod |
| **프로젝트 단계** | 초기 세팅 단계 (스타터/보일러플레이트) |

소스 파일 **8개**로 구성된 초기 프로젝트. 실제 비즈니스 로직은 아직 없고, 기반 구조만 잡혀 있는 상태.

---

## 2. 프로젝트 구조 분석

```
src/
├── app/
│   ├── layout.tsx        # 루트 레이아웃 (Providers 래핑)
│   ├── page.tsx          # 랜딩 페이지 (소개 UI)
│   └── globals.css       # Tailwind v4 + shadcn 테마 변수
├── components/
│   ├── providers.tsx     # QueryClientProvider
│   └── ui/               # (비어 있음 - shadcn 컴포넌트 설치 대기)
├── constants/
│   └── site.ts           # 사이트 설정 상수
├── features/             # (비어 있음 - Feature 기반 구조 예정)
├── hooks/
│   └── use-mounted.ts    # SSR 하이드레이션 체크 훅
├── lib/
│   └── utils.ts          # cn() 유틸리티 (clsx + tailwind-merge)
└── remote/
    └── client.ts         # HTTP 클라이언트 (fetch 래퍼)
```

### 구조 평가

**잘 된 점:**

- Feature 기반 디렉토리 구조 (`features/`) 준비됨 - 확장성 있음
- `remote/client.ts` - 깔끔한 HTTP 클라이언트 추상화
- `providers.tsx` - QueryClient를 `useState`로 관리 (SSR 안전)
- `@/*` path alias 설정 완료

**개선 포인트:**

- `features/` 디렉토리가 비어 있음 - 향후 기능 추가 시 구조 가이드 필요
- `stores/` 디렉토리 없음 - Zustand 스토어 관리 공간 필요
- `types/` 디렉토리 없음 - 공유 타입 정의 공간 필요
- App Router 필수 파일 누락 — `loading.tsx`, `error.tsx`, `not-found.tsx` 추가 필요

### 권장 디렉토리 구조 (확장 시)

```
src/
├── app/                    # App Router 라우트 및 레이아웃
│   ├── (auth)/            # 라우트 그룹 (인증 관련)
│   ├── (dashboard)/       # 라우트 그룹 (대시보드)
│   ├── api/               # API 라우트 핸들러
│   ├── layout.tsx
│   └── page.tsx
├── components/
│   ├── ui/                # shadcn/ui 기본 UI 컴포넌트
│   └── common/            # 프로젝트 공통 컴포넌트
├── features/              # 기능별 모듈 (각 feature 내에 components, hooks, types 포함)
├── hooks/                 # 전역 커스텀 Hooks
├── lib/                   # 유틸리티, 설정, 헬퍼
├── remote/                # API 클라이언트
├── stores/                # Zustand 스토어
├── types/                 # 공유 TypeScript 타입
└── constants/             # 상수 정의
```

---

## 3. 라이브러리 버전 분석

### 긴급도별 분류

#### 🔴 높음 - 즉시 조치 권장

| 라이브러리 | 현재 버전 | 최신 버전 | 이슈 |
|-----------|----------|----------|------|
| **react-use** | ^17.6.0 | 17.6.0 | 유지보수 사실상 중단. 1년 이상 업데이트 없음. React 19 호환성 미보장. 현재 미사용이나 **아키텍처 룰에 필수 라이브러리로 지정됨** → 대안 라이브러리로 교체 검토 필요 |
| **Zod** | ^3.24.2 | **4.3.6** | v4에서 파싱 성능 6~14배 향상, 번들 크기 감소. 점진적 마이그레이션 가능 (`zod/v4` 서브패스) |

#### 🟡 중간 - 계획적 업그레이드 권장

| 라이브러리 | 현재 버전 | 최신 버전 | 이슈 |
|-----------|----------|----------|------|
| **Next.js** | ^15.1.7 | **16.1.6** | Turbopack 기본 활성화, React Compiler 안정화, `proxy.ts` 도입 |
| **React** | ^19.0.0 | **19.2.4** | `<Activity/>`, View Transitions, `useEffectEvent()`, 보안 패치 |

#### 🟢 낮음 - 자동 업데이트 범위 내

| 라이브러리 | 현재 버전 | 최신 버전 | 상태 |
|-----------|----------|----------|------|
| Tailwind CSS | ^4.0.6 | 4.1.18 | text-shadow, mask 유틸리티 추가 |
| @tanstack/react-query | ^5.66.0 | 5.90.20 | 안정적 |
| Zustand | ^5.0.3 | 5.0.11 | 안정적 |
| react-hook-form | ^7.54.2 | 7.71.1 | v8 beta 진행 중, v7 유지 권장 |
| shadcn/ui | ^3.8.4 | 3.8.4 | 최신 |
| radix-ui | ^1.4.3 | 1.4.3 | 최신 (통합 패키지) |
| es-toolkit | ^1.32.0 | 1.44.0 | Lodash 대비 2배 빠름, 97% 작음 |
| ts-pattern | ^5.7.0 | 5.9.0 | 안정적 |
| dayjs | ^1.11.13 | 1.11.13 | Temporal API 모니터링 (2027년 이후 대체 가능) |

---

## 4. 라이브러리별 상세 분석

### 4.1 Next.js (^15.1.7 → 16.1.6)

**Next.js 16 주요 변경 사항:**

- **Turbopack 안정화:** `next dev`와 `next build` 모두에서 기본 번들러
- **React Compiler 안정화:** 자동 메모이제이션 (`useMemo`, `useCallback` 불필요)
- **React 19.2 지원:** View Transitions, `useEffectEvent()`, `<Activity/>` 등
- **`proxy.ts` 도입:** `middleware.ts`가 `proxy.ts`로 변경, Node.js 런타임 실행
- **향상된 라우팅:** 레이아웃 중복 제거, 증분 프리페칭

**업그레이드 방법:**

```bash
npx @next/codemod@canary upgrade latest
# 또는
npm install next@latest react@latest react-dom@latest
```

### 4.2 React (^19.0.0 → 19.2.4)

**React 19.2 주요 기능:**

- **`<Activity/>`:** UI를 숨기되 상태 보존 (탭, 모달 등)
- **`useEffectEvent()`:** Effect 내 이벤트 로직 분리
- **View Transitions:** 선언적 애니메이션
- **Server Actions DoS 방어 강화** (보안 패치)

### 4.3 Zod (^3.24.2 → 4.3.6)

**v4 성능 향상:**

| 파싱 유형 | 성능 향상 |
|----------|----------|
| 문자열 | 14배 |
| 배열 | 7배 |
| 객체 | 6.5배 |

**주요 API 변경:**

- `.merge()` deprecated
- `error.errors` → `error.issues`
- `._def` → `._zod.def`
- `ZodTypeAny` 제거 → `ZodType` 사용

**점진적 마이그레이션:**

```typescript
// v3와 v4 공존 가능
import { z } from "zod/v4";

// 자동 codemod
npx zod-v3-to-v4
```

### 4.4 react-use (^17.6.0) - 교체 권장

**문제:** 유지보수 사실상 중단 (1년 이상 업데이트 없음)

**추천 대안:**

| 대안 | 특징 |
|------|------|
| **@uidotdev/usehooks** (1순위) | 경량, SSR 호환, 활발한 유지보수 |
| **ahooks** (2순위) | 포괄적 70+ hooks, 복잡한 앱에 적합 |
| **usehooks-ts** (3순위) | TypeScript 네이티브 |

> **참고:** 현재 프로젝트에서 react-use를 실제 import하는 파일이 없음. 즉시 `npm uninstall react-use`로 제거 가능.

### 4.5 dayjs vs 대안

| 라이브러리 | 번들 크기(gzip) | 특징 |
|-----------|---------------|------|
| **dayjs** (현재) | ~6kb | Moment.js 호환 API, 가벼움 |
| **date-fns** | ~18kb (로케일 미포함) | Tree-shaking 우수, 함수형 |
| **Temporal API** | 0kb (네이티브) | 표준 API, 2027년 이후 프로덕션 사용 가능 예상 |

dayjs 유지 권장. Temporal API는 Chrome 144에서 지원 시작했으나 아직 전 브라우저 안정 지원 아님.

### 4.6 tw-animate-css (현재 사용 중)

Tailwind v4에서 가장 권장되는 애니메이션 솔루션. shadcn/ui에서 기본 설치. v2.0.0 출시 예정.

---

## 5. 코드 품질 분석

### 잘 된 점

- `remote/client.ts` — 제네릭 타입을 활용한 깔끔한 HTTP 클라이언트
- `providers.tsx` — QueryClient를 `useState`로 초기화 (서버/클라이언트 인스턴스 분리)
- Tailwind v4 + shadcn/ui 최신 세팅 (oklch 색상, CSS-first 설정)
- `"use client"` 지시어가 프로젝트 룰(`global.mdc`)에 따라 모든 컴포넌트에 적용됨

### 개선 포인트

> **참고:** 프로젝트 룰(`global.mdc`)에 "always use client component for all components" 규칙이 있으므로, `page.tsx`의 `"use client"`는 규칙 준수 상태임. ~~(초기 분석에서 제거를 권장했으나 정정함)~~

**2. `remote/client.ts` 에러 핸들링 및 환경변수 안전성**

```typescript
// 현재: 단순 Error throw
throw new Error(`HTTP Error: ${response.status} ${response.statusText}`);

// 권장: 응답 본문 파싱, 커스텀 에러 클래스 사용
```

```typescript
// 위험: NEXT_PUBLIC_API_URL이 빈 문자열("")이면 new URL("")이 예외를 던질 수 있음
const BASE_URL = process.env.NEXT_PUBLIC_API_URL ?? "";
const url = new URL(`${BASE_URL}${endpoint}`); // TypeError 가능
```

**3. react-use 유지보수 상태 우려**

`react-use`는 `architecture.mdc`에 필수 라이브러리로 지정됨. 현재 실제 import는 없으나 향후 사용 예정. 다만 **1년 이상 업데이트 없이 유지보수 중단 상태**이므로, 아키텍처 룰 자체를 업데이트하여 대안 라이브러리(`@uidotdev/usehooks` 등)로 교체를 검토할 필요 있음.

**4. ESLint flat config 누락**

ESLint 9가 설치되어 있으나 루트에 `eslint.config.mjs`가 없어 린트 구성이 미완성 상태.

```javascript
// eslint.config.mjs (추가 필요)
import { dirname } from "path";
import { fileURLToPath } from "url";
import { FlatCompat } from "@eslint/eslintrc";

const __filename = fileURLToPath(import.meta.url);
const __dirname = dirname(__filename);

const compat = new FlatCompat({ baseDirectory: __dirname });

export default [
  ...compat.extends("next/core-web-vitals", "next/typescript"),
];
```

**5. 환경 변수 검증 부재**

Zod 기반 env 스키마로 빌드/런타임 시점에 환경 변수를 검증하는 패턴 권장.

```typescript
// src/lib/env.ts (추가 권장)
import { z } from "zod";

const envSchema = z.object({
  NEXT_PUBLIC_API_URL: z.string().url().optional(),
});

export const env = envSchema.parse({
  NEXT_PUBLIC_API_URL: process.env.NEXT_PUBLIC_API_URL,
});
```

**6. TypeScript target 상향 가능**

현재 `ES2017` → `ES2022`로 올리면 `top-level await`, `Object.hasOwn`, `Array.at()` 등 최신 API의 타입 지원을 받을 수 있음. Next.js가 SWC로 트랜스파일하므로 런타임 호환성에는 영향 없음.

---

## 6. 기술 스택 적합성 평가

| 영역 | 선택 | 평가 | 비고 |
|------|------|------|------|
| 프레임워크 | Next.js 15 | ✅ 우수 | 16으로 업그레이드 계획 필요 |
| UI 라이브러리 | shadcn/ui + Radix | ✅ 우수 | 2026년 최고 인기 조합 |
| 스타일링 | Tailwind v4 | ✅ 우수 | CSS-first 최신 패러다임 |
| 서버 상태 | TanStack Query | ✅ 우수 | 업계 표준 |
| 클라이언트 상태 | Zustand | ✅ 우수 | 경량 + 강력 |
| 폼 | react-hook-form + Zod | ✅ 우수 | 가장 인기 있는 조합 |
| 유틸리티 | es-toolkit | ✅ 우수 | Lodash 대체 최적 |
| 패턴 매칭 | ts-pattern | ✅ 우수 | TypeScript 패턴 매칭 표준 |
| 날짜 | dayjs | ✅ 적절 | 경량, 안정적 |
| Hooks | react-use | ⚠️ 교체 검토 | 아키텍처 룰 지정이나 유지보수 중단 상태 |

---

## 7. 액션 플랜 (우선순위순)

### High — 즉시 조치

| 순위 | 작업 | 난이도 | 영향도 | 상태 |
|------|------|--------|--------|------|
| 1 | `eslint.config.mjs` 추가 (ESLint flat config) | 낮음 | 중 | ⬜ 대기 |
| 2 | `react-use` 대안 라이브러리 교체 검토 (아키텍처 룰 업데이트 포함) | 낮음 | 중 | ⬜ 대기 |
| 3 | 환경 변수 검증 추가 (`src/lib/env.ts`) | 낮음 | 중 | ⬜ 대기 |
| 4 | HTTP 클라이언트 빈 URL 예외 처리 | 낮음 | 중 | ⬜ 대기 |

### Medium — 단기 계획

| 순위 | 작업 | 난이도 | 영향도 | 상태 |
|------|------|--------|--------|------|
| 5 | Zod v3 → v4 마이그레이션 | 낮음 | 높음 | ⬜ 대기 |
| 6 | TypeScript target `ES2017` → `ES2022` 상향 | 매우 낮음 | 낮음 | ⬜ 대기 |
| 7 | App Router 필수 파일 추가 (`loading.tsx`, `error.tsx`, `not-found.tsx`) | 낮음 | 중 | ⬜ 대기 |
| 8 | `stores/`, `types/` 디렉토리 구조 확립 | 낮음 | 중 | ⬜ 대기 |

### Low — 중기 계획

| 순위 | 작업 | 난이도 | 영향도 | 상태 |
|------|------|--------|--------|------|
| 9 | Next.js 15 → 16 업그레이드 | 중간 | 높음 | ⬜ 대기 |
| 10 | React 19.0 → 19.2 업데이트 | 낮음 | 중 | ⬜ 대기 |
| 11 | 테스트 인프라 도입 (Vitest + RTL) | 중간 | 높음 | ⬜ 대기 |
| 12 | Prettier + lint-staged + Husky 구성 | 낮음 | 중 | ⬜ 대기 |

---

## 8. 타 AI 분석과의 비교 (Cross-Review)

> 타 AI(Cursor)의 `PROJECT_ANALYSIS.md` 분석 결과와 본 분석을 교차 검증한 결과.
> 2차 검증 시 `.claude/rules/` 프로젝트 룰 파일 반영하여 정정.

### 🔴 타 AI 오류 — 반박 유지

**1. "`radix-ui`는 실존 패키지명이 아니므로 제거 필요"**

**완전히 틀림.** `radix-ui`는 2025년에 출시된 공식 통합 패키지. npm 레지스트리에 `radix-ui@1.4.3` 공식 등록, `node_modules/radix-ui/package.json`에서 확인됨. 기존 `@radix-ui/react-*` 개별 패키지들이 단일 패키지로 통합된 것이며, shadcn/ui new-york 스타일에서 기본 사용. **제거하면 안 됨.** 타 AI는 2차 반박에서도 이 주장을 유지하고 있으나 객관적 사실에 반함.

**2. "HTTP 클라이언트를 `ky`나 `axios`로 교체 고려"**

**불필요.** 2026년 기준 브라우저/Node.js의 `fetch` API가 완전히 성숙. Next.js는 `fetch`를 확장하여 캐싱/재검증 기능을 내장. `axios`나 `ky`는 Next.js의 fetch 확장 기능과 충돌하거나 번들 크기만 증가. 현재의 fetch 기반 경량 래퍼가 최적.

### 🟡 본 분석 정정 — 프로젝트 룰 미반영 오류

`.claude/rules/` 하위 프로젝트 룰 파일들을 초기 분석 시 읽지 않아 발생한 오류:

**1. "`page.tsx`에서 `"use client"` 제거" 권장 → 정정: 프로젝트 규칙 준수 상태**

`global.mdc` 11행에 `"always use client component for all components (use "use client" directive)"` 규칙이 명시됨. `page.tsx`의 `"use client"`는 이 규칙을 따른 것이므로 제거 대상이 아님. **타 AI의 반박이 정당함.**

**2. "`react-use` 즉시 제거 가능" → 정정: 아키텍처 룰에 필수 라이브러리로 지정됨**

`architecture.mdc` 17행에 `"react-use: For commonly needed React hooks."` 로 필수 라이브러리 목록에 포함됨. 현재 미사용이더라도 즉시 삭제 대상이 아니라, 아키텍처 룰 업데이트와 함께 대안 라이브러리로의 교체를 검토해야 함. **타 AI의 반박이 정당함.**

### 🟢 타 AI 유효 포인트 — 수용하여 병합함

| 항목 | 내용 | 병합 위치 |
|------|------|----------|
| ESLint flat config 누락 | ESLint 9 설치됐으나 `eslint.config.mjs` 없음 | 5장 개선 포인트 #4, 액션 플랜 #1 |
| 환경 변수 검증 | `NEXT_PUBLIC_API_URL` 빈 문자열 시 `new URL()` 예외 가능 | 5장 개선 포인트 #2, 액션 플랜 #3, #4 |
| TypeScript target 상향 | `ES2017` → `ES2022`로 최신 API 타입 지원 | 5장 개선 포인트 #6, 액션 플랜 #6 |
| App Router 파일 추가 | `loading.tsx`, `error.tsx`, `not-found.tsx` 누락 | 2장 개선 포인트, 액션 플랜 #7 |

### 📊 종합 비교 (2차 정정 반영)

| 분석 항목 | 타 AI | 본 분석 (초기) | 본 분석 (정정 후) | 최종 판정 |
|----------|-------|--------------|----------------|----------|
| radix-ui 제거 | ❌ 제거 필요 | ✅ 정상 패키지 | ✅ 정상 패키지 | **타 AI 오류 (npm 확인됨)** |
| HTTP 클라이언트 교체 | ky/axios 고려 | fetch 래퍼 유지 | fetch 래퍼 유지 | **타 AI 과잉 제안** |
| `"use client"` 제거 | ✅ 규칙 위반 지적 | ❌ 제거 권장 | ✅ 규칙 준수 인정 | **타 AI가 맞음 → 정정** |
| react-use 즉시 제거 | ✅ 규칙상 유지 | ❌ 즉시 제거 | ⚠️ 교체 검토 | **타 AI가 맞음 → 정정** |
| ESLint config 누락 | ✅ 지적 | (누락) | ✅ 병합 | **타 AI가 더 나음** |
| 환경변수 검증 | ✅ 지적 | (누락) | ✅ 병합 | **타 AI가 더 나음** |
| TS target 상향 | ✅ ES2022+ | (누락) | ✅ 병합 | **타 AI가 더 나음** |
| App Router 파일 | ✅ 지적 | (누락) | ✅ 병합 | **타 AI가 더 나음** |
| react-use 유지보수 중단 | ❌ 미언급 | ✅ 지적 | ✅ 교체 검토 | **본 분석이 더 나음** |
| Zod v4 | ❌ 미언급 | ✅ 업그레이드 권장 | ✅ 유지 | **본 분석이 더 나음** |
| Next.js 16 | ❌ 미언급 | ✅ 업그레이드 계획 | ✅ 유지 | **본 분석이 더 나음** |
| React 19.2 | ❌ 미언급 | ✅ 신규 API 소개 | ✅ 유지 | **본 분석이 더 나음** |
| 최신 버전 전수 조사 | ❌ 없음 | ✅ 13개 조사 | ✅ 유지 | **본 분석이 더 나음** |

---

## 9. 프로젝트 룰 요약

> `.claude/rules/` 하위 파일에서 확인된 프로젝트 규칙. 분석 시 반드시 참조해야 함.

### `global.mdc` 핵심 규칙

- **모든 컴포넌트에 `"use client"` 사용** (Server Component 사용 안 함)
- `page.tsx` params props는 반드시 Promise 사용
- 플레이스홀더 이미지는 picsum.photos 사용
- shadcn-ui 컴포넌트 추가 시 설치 명령어만 안내 (사용자가 직접 설치)
- 패키지 매니저는 npm 사용
- Tailwind CSS v4 필수

### `architecture.mdc` 핵심 규칙

- **필수 라이브러리:** dayjs, ts-pattern, @tanstack/react-query, zustand, react-use, es-toolkit, lucide-react, zod, shadcn-ui, tailwindcss, react-hook-form
- **파일명은 kebab-case** (예: `feed-page.tsx`, `use-posts.ts`)
- Feature 기반 디렉토리 구조 (`src/features/[featureName]/`)

### `new-feature-process.mdc` 핵심 규칙

- API 요청은 `src/features/[featureName]/api.ts`에 정의, `fetch` 직접 사용 금지
- 스키마는 `src/features/[featureName]/schema.ts`에 정의
- API 상태 관리는 react-query 사용 (`useState`/`useEffect` 금지)

---

## 10. 총평

전체적으로 **매우 잘 구성된 모던 스타터 프로젝트**. 핵심 기술 스택 선택이 2026년 트렌드에 잘 부합하며, 즉시 필요한 조치:

1. **`eslint.config.mjs` 추가** — ESLint 9 flat config 설정
2. **`react-use` 대안 검토** — 아키텍처 룰에 지정되어 있으나 유지보수 중단 상태이므로, 룰 업데이트와 함께 `@uidotdev/usehooks` 등으로 교체 검토
3. **환경 변수 검증** — Zod 기반 `src/lib/env.ts` 추가
4. **Zod v4 업그레이드** — 성능 6~14배 향상, 점진적 마이그레이션 가능

중기적으로 Next.js 16 업그레이드를 계획하면 React Compiler, Turbopack 기본 활성화 등의 혜택을 받을 수 있음.

---

> **정정 이력:**
> - 초기 분석(2026-02-07): `.claude/rules/` 프로젝트 룰 미반영
> - 1차 병합: 타 AI의 유효 포인트 4건 병합 (ESLint, 환경변수, TS target, App Router 파일)
> - 2차 정정: 프로젝트 룰 반영 — `"use client"` 제거 권장 철회, `react-use` 즉시 제거 → 교체 검토로 변경
